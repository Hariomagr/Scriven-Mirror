<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dellpc.scrivenmirror">

    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/k"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/k"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity android:name=".Homescreen" />
        <activity
            android:name=".Mainclass"
            android:label="@string/title_activity_mainclass"
            android:theme="@style/NavDrawer" />
        <activity android:name=".submissionadmin" />
        <activity android:name=".Submistatus" />
        <activity android:name=".submit" />
        <activity android:name=".sendd" />
        <activity android:name=".mytale" />
        <activity android:name=".devecl"></activity>
    </application>

</manifest>